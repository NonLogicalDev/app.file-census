# File Census Rebuild Goals

## Recovery status

On 2026-07-14 the prior repository, Git history, plans, source, and generated assets were deleted during disk cleanup. Structured historical session logs survive and are being replayed only when their context can be validated exactly. A 109-event provenance index plus complete direct-read seeds now produces hash-verified source paths; a final-architecture baseline has been promoted into this checkout after static React import and Rust module checks. The global stylesheet has been recovered through a clean 2026-07-12 horizon, and the later June 14 App source is now restored byte-for-byte with a 47-file relative-import closure. Exact root `flake.nix` and `.gitignore` have been restored. A working-tree `flake.lock` appeared later, but it has no validated provenance and must remain unstaged until the approved build-input audit; it is not canonical evidence. A 41-file historical plan archive, including per-file direct-event provenance and SHA-256 checks, is restored under `agent-plans/recovered/historical/`; no original `.agent-plans/` project entries exist in the evidence. The configured macOS PNG/ICNS icon assets are now recovered from the installed File Census bundle, with the ICNS copied byte-for-byte. The rebuilt exclusion foundation now has snapshot-scoped tree/search/occurrence pages, protected visible deletion, scan-root containment, scan-relative media resolution, scan-aware file/folder action authorization, per-candidate thumbnail reauthorization, and browser/native action-contract parity tests in source. Scanner workers now have source-level ordinary-error finalization, failed-summary terminal progress parity, transactional progress-count/cache updates, and a narrowly guarded terminal-state fallback when cache deletion fails; retained cache runs become stale by their terminal fingerprint. Workers also have bounded late-symlink/replacement rechecks and descriptor-derived metadata. This is not a portable descriptor-relative no-follow proof, and crash/panic stale-run reconciliation remains an explicit later policy. Static production UI closure now includes the directory-only File tree, real top-bar Cmd-K dispatch, persisted/resizable sidebar, scan-scoped folder browsing, and all required isolated preview entries; unsupported pause/resume/repair actions are intentionally unavailable rather than fake transport calls. The root build recipes now acquire the declared Nix development shell for both frontend and Rust commands instead of depending on an ambient Node installation. The bundled Node source suite passes 138/138 tests, but that is not a Vite, backend, or native build. The compatible two-positional CLI scan path now validates source/offset before a single database transaction creates a compatible Unknown location when needed and reserves its UTC date-derived scan ID; scanner progress now has bounded oldest-first active-operation telemetry, while the Ratatui renderer remains pending. A fresh read-only audit confirms `nix` and `just` are present but Node/npm/Cargo/Rust are absent, `ui/dist` is empty, and no backend/native executable or `.app` exists. Builds therefore remain explicitly gated until canonical inputs are deliberately restored and exercised. Full macOS `.app` packaging also needs full Xcode rather than Command Line Tools. The designated in-app browser connects, but enterprise policy blocks the local preview origin, so screenshots cannot be truthfully captured without an allowed origin; no Chrome fallback is permitted. Every patch is gated by matching direct success output, so failed historical calls are excluded. Formatter-conflicted backend revisions remain unresolved, so the checkout is not yet accepted as runnable and every historic implementation/verification claim remains reset.

## Completion contract

- [ ] Rebuild the approved shadcn-inspired dark production UI with real application behavior—not a mock or a reskin.
- [ ] Recover all deterministically replayable source from session logs without fuzzy patches, copied-context duplicates, or post-deletion events; rebuild only the unrecoverable remainder.
- [ ] Match the recovered/re-supplied visual reference at 1440x900 and 1280x800: compact near-black surfaces, restrained one-pixel borders, dense operational layout, sparse semantic color, no gradients or ornamental cards.
- [ ] Preserve real application state, JSON-RPC, WebSocket/Tauri transport, backend behavior, routing, destructive confirmations, and file workflows.
- [ ] Rebuild the collapsible/resizable sidebar, main-top-bar Cmd-K, Locations/Scan Browser, Files/File tree, Duplicates, Tasks, Search, Options, and safe file actions.
- [ ] Enforce directory-only scan navigation; files render only in the main results workspace.
- [ ] Implement persistent, non-destructive per-scan excludes for every scan-scoped query and action, with source/index integrity proof.
- [ ] Add the compatible CLI scan shorthand: database location + source path + volume slug bootstrap missing location/scan records without altering existing command behavior.
- [ ] Add an opt-in Ratatui progress workflow with truthful queue/pool bars and oldest-first active operations, safe terminal restoration, and non-TTY fallback.
- [ ] Maintain synchronized plans and `agent-plans/workqueue.yaml`; checkpoint stable milestones.
- [ ] Complete automated, browser-server, native/Tauri, accessibility, destructive-fixture, and design-QA evidence with no unresolved P0/P1/P2 issues.
- [ ] Receive explicit user approval of final production screenshots and behavior.

## Missing acceptance input

- [ ] User re-supplies `shadcn-dark-reference.jpg` and the approved prototype baseline/screenshots, which were deleted with the repository. Do not invent visual acceptance evidence without them.
